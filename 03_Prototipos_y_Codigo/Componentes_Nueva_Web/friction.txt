    // Dónde se estancan los proyectos (Fricción Operativa) - Estilo Bento Grid
    const AgenticFrictionPoints = () => {
      const pains = [
        {
          id: 'vision',
          icon: '🛑',
          title: 'La visión está aprobada. Nada opera en producción.',
          desc: 'Las consultoras tradicionales entregan powerpoints y diagramas. Tu equipo sigue haciendo el trabajo manual porque nadie sabe cómo conectar un LLM a tu ERP.'
        },
        {
          id: 'tech',
          icon: '⚠️',
          title: 'Deuda técnica agentica',
          desc: 'Agentes construidos con prompts frágiles que se rompen cuando la API cambia, sin observabilidad ni validación humana.'
        },
        {
          id: 'ux',
          icon: '📉',
          title: 'Rechazo del usuario final',
          desc: 'Herramientas internas complejas que los operadores se niegan a usar porque la interfaz no resuelve su flujo real de trabajo diario.'
        }
      ];

      return (
        <section id="friction" className="py-24 px-6 md:px-12 bg-navy-950 border-t border-white/[0.06]">
          <div className="max-w-7xl mx-auto">
            <div className="mb-16">
              <div className="text-[11px] font-mono uppercase tracking-widest text-emerald-400 font-bold mb-4">POR QUÉ SE ESTANCAN LOS PROYECTOS</div>
              <h2 className="text-4xl md:text-5xl font-black text-white tracking-tight leading-[1.1] mb-4">
                El abismo entre el PoC y la Operación<span className="text-blue-500">.</span>
              </h2>
              <p className="text-slate-400 text-base md:text-lg max-w-2xl">
                La mayoría de iniciativas de IA mueren antes de tocar datos reales. Estos son los tres modos de falla que BluePixel resuelve estructuralmente.
              </p>
            </div>

            <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
              {pains.map((p, idx) => (
                <div key={idx} className={g-navy-900 border border-white/[0.08] rounded-3xl p-8 flex flex-col justify-between hover:border-blue-500/30 transition-colors }>
                  <div>
                    <div className="w-12 h-12 rounded-xl bg-navy-950 flex items-center justify-center text-2xl mb-6 shadow-inner border border-white/[0.04]">
                      {p.icon}
                    </div>
                    <h3 className="text-2xl font-bold text-white mb-3 tracking-tight">{p.title}</h3>
                    <p className="text-slate-400 text-sm md:text-base leading-relaxed">{p.desc}</p>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </section>
      );
    };